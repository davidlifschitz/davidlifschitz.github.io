# David Lifschitz

Personal landing page for GitHub Pages.

## Ecosystem dashboard

The site now includes an ecosystem change dashboard at `dashboard.html` that tracks daily lines changed per repo.

Public repos can update immediately from the scheduled workflow. Private repos will populate once an `ECOSYSTEM_GH_TOKEN` repository secret with cross-repo read access is added to this repo.
